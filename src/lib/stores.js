import { writable } from "svelte/store";

//værdier overskrives i Table.svelte
// export let count_using_stores = writable([1, 2, 3, 4, 5, 0, 7, 8, 9, 15]);
export let count_using_stores = writable([]);
