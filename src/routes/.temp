 <a class="nav-link {i === 0 ? `active" aria-current="page" href="/"` : `" href="{navItem.title}"`} >{navItem.title} {#if navItem.count}({navItem.count}){/if}</a>



      {#if i === 0}
                <a class="nav-link active" aria-current="page"  href="/" >
                    {navItem.title} {#if navItem.count}({navItem.count}){/if}
                </a>
                {:else}
                    <a class="nav-link" href="{navItem.title}">
                        {navItem.title} {#if navItem.count}({navItem.count}){/if}
                </a>
                {/if}